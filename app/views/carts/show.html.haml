%h1 Shopping cart
.row
  - if @order_items.any?
    - @order_items.each do |item|
      .col.s12.m6
        .card.blue-grey.darken-1
          .card-content.white-text
            %span.card-title
              = item.product.name
            %p
              Unit Price: #{number_to_currency item.product.price}
              %br Quantity: #{item.quantity}
              %br Total Price: #{number_to_currency (item.product.price * item.quantity)}
          .card-action
            %a= link_to 'Show', item.product
            %a= link_to 'Delete', order_item_path(item), method: :delete, data: { confirm: 'Are you sure?' }
  - else
    .col.s12.m6
      .card.blue-grey.darken-1
        .card-content.white-text
          Your shopping cart is empty.

%br/