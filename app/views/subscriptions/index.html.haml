%h1 Subscriptions
.row
  - @subscriptions.each do |subscription|
    .col.s12.m6
      .card.blue-grey.darken-1
        .card-content.white-text
          %span.card-title
            =subscription.name
          %p
            I'm a subscription! My product is #{subscription.product.name}.
        .card-action
          %a= link_to 'Show', subscription
          %a= link_to 'Edit', edit_subscription_path(subscription)
          %a= link_to 'Destroy', subscription, method: :delete, data: { confirm: 'Are you sure?' }
%br/

= link_to 'New subscription', new_subscription_path, class: "waves-effect waves-light btn"
